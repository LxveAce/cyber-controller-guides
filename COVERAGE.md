# Guide coverage & backlog

Tracks which of Cyber Controller's shipped flash **profiles** have a written hardware guide, and seeds the ones that
don't with the authoritative facts needed to write them. The published guides themselves are listed in
[README.md](README.md) (auto-generated by `tools/build_readme.py`); this file is the **inverse** — what's still
missing — so contributors know where to help.

- **Shipped profiles:** 50 (`cyber-controller/src/config/profiles/*.json` — the SSOT).
- **Published guides:** 30 = **26 profile guides + 3 bootable-OS guides (Arch, Kali USB, Tails) + 1 detector guide
  (Jammer Detection)**. The OS and detector guides sit beyond the flash-profile set.
- **Profiles without a guide yet:** 24 (below).

> Keep the "50" in sync with the app and with `APP_PROFILE_COUNT` in `tools/build_readme.py` whenever Cyber
> Controller adds or drops a profile.
>
> The facts below are pulled straight from each profile's JSON (backend / board / chip / upstream repo) and are safe to
> rely on. A full guide is more than facts — the **buy → build → flash → integrate → troubleshoot** walkthrough must be
> **verified against the real device** before publishing (the repo's `verify:` discipline). Those steps are marked TODO.
> Several of these are offensive/regulated (deauth, BadUSB) — their guides must lead with the **authorized-use / your-own-
> gear** frame, same as the existing ⚠ entries. The two lab-only jammer profiles must stay **flash-and-study only**: no
> transmit or wiring/build content, matching how the BlueJammer entries are handled.

## Backlog — profiles needing a guide

| Profile (id) | Category | Backend | Board / chip | Upstream | Notes for the guide author |
|---|---|---|---|---|---|
| `ble_collector` | ESP32 (M5) | esptool | M5Stack Core2 / Fire / Basic / CoreS3, Odroid-GO, esp32/esp32s3, 16MB | [tobozo/ESP32-BLECollector](https://github.com/tobozo/ESP32-BLECollector) | Passive BLE advertisement logger to SD. Detector-only (🛡) — no offensive actions. |
| `bluestress` | ESP32 | esptool | ESP32-WROOM-32 (NodeMCU / DevKit) + nRF24L01 | [LxveAce/BlueStress](https://github.com/LxveAce/BlueStress) | ⚠ **LAB-ONLY / illegal to operate** (FCC 47 U.S.C. §333). Flash-and-study only — no transmit or wiring/build content; lead with the lawful-use frame, same as the BlueJammer guides. |
| `catsniffer` | Multiprotocol RF sniffer (passive) | cc2538_bsl (+ uf2 bridge) | CatSniffer V3 — CC1352P7 radio (cc2538_bsl) + RP2040 USB bridge (uf2) | [ElectronicCats/CatSniffer-Firmware](https://github.com/ElectronicCats/CatSniffer-Firmware) | Passive 802.15.4/Zigbee/Thread + BLE + sub-GHz sniffer with live Wireshark (extcap) capture (🛡 — no attack; the bundled airtag_spoofer active-TX build is excluded). Two-chip device: BOTH the CC1352P7 sniffer (cc2538_bsl) and the RP2040 USB bridge (uf2) must be flashed for a working sniffer. First guide to exercise the new `cc2538_bsl` backend; keep app_offset / baud / SHA `verify:` until a real CatSniffer flash. |
| `drone_mesh_mapper` | ESP32 | esptool | Seeed Xiao ESP32-C3 / ESP32-S3 (dual) | [colonelpanichacks/drone-mesh-mapper](https://github.com/colonelpanichacks/drone-mesh-mapper) | Passive WiFi Drone Remote-ID detector (🛡 lawful airspace monitoring). |
| `esp32_wardriver` | ESP32 | esptool | Espressif ESP32-C5-DevKitC-1 v1.2, esp32c5, 4MB | [justcallmekoko/ESP32DualBandWardriver](https://github.com/justcallmekoko/ESP32DualBandWardriver) | Passive dual-band WiGLE logging. Note wardriving legality varies by jurisdiction. |
| `esp32_wifi_pentest` | ESP32 | esptool | ESP32 DevKit (WROOM), esp32, 4MB | [risinek/esp32-wifi-penetration-tool](https://github.com/risinek/esp32-wifi-penetration-tool) | Wi-Fi pentest suite; **authorized-use frame** (deauth/handshake capture). HW-validated on COM19 per the app. |
| `esp8266_deauther` | ESP8266 | esptool | Wemos D1 mini / NodeMCU / DSTIKE, esp8266, 4MB | [SpacehuhnTech/esp8266_deauther](https://github.com/SpacehuhnTech/esp8266_deauther) | Classic Spacehuhn deauther; **authorized-use frame** (⚠ offensive). Distinct from Hydra32 (ESP32-Deauther). |
| `esp_at` | ESP32 | esptool | ESP32-WROOM / WROVER / S2 / S3 / C3 (per-board factory .bin) | [espressif/esp-at](https://github.com/espressif/esp-at) | Espressif AT-command Wi-Fi/BT firmware. Note the per-board factory image + offsets. |
| `flipper_roguemaster` | Flipper Zero | qflipper | Flipper Zero, stm32wb55, 1MB | [RogueMaster/flipperzero-firmware-wPlugins](https://github.com/RogueMaster/flipperzero-firmware-wPlugins) | Same install model as Momentum/Unleashed (package → qFlipper); no multi-file esptool flow. |
| `lxveos` | ESP32 security-panel OS (defensive) | esptool | Bare ESP32 / CYD 2.8" / CYD 3.5" / M5StickC Plus2 (esp32); M5Cardputer / JC3248W535 (esp32s3); per-board merged `.bin` @ 0x0 | [LxveAce/lxveos](https://github.com/LxveAce/lxveos) | LxveAce security-panel OS (ESP-IDF) — passive WiFi/BLE recon + defensive detectors, EAPOL/PMKID capture, an arm-gated offensive set, and the LXVEOS/1 serial event bridge; **authorized/defensive use**. Tracks the rolling `ci-latest` GitHub prerelease channel. Flash write is owner-gated until HARDWARE-CLAIMS validation (bare ESP32 headless HW-validated on ESP32-D0WD). |
| `m5gotchi` | ESP32 (M5) | esptool | M5Cardputer / M5Stick-S3, esp32s3, 8MB | [Devsur11/M5Gotchi](https://github.com/Devsur11/M5Gotchi) | Pwnagotchi-style on M5; keyboard/display board variants. |
| `m5stick_nemo` | ESP32 (M5) | esptool | M5StickC Plus2 (esp32) / M5Cardputer / M5Stick-S3 (esp32s3), 8MB | [n0xa/m5stick-nemo](https://github.com/n0xa/m5stick-nemo) | Multi-tool for M5 sticks; note per-board chip + flash_mode (dio vs qio) differences. |
| `mayhem` | HackRF / PortaPack SDR firmware | hackrf_spiflash | HackRF One + PortaPack H1/H2/H4M, PortaRF, HackRF Pro (experimental) — lpc43xx | [portapack-mayhem/mayhem-firmware](https://github.com/portapack-mayhem/mayhem-firmware) | ⚠ **illegal-tx label** — bundles TX apps on protected/emergency bands, but genuinely dual-use with a large legit RX side (label-and-warn, never block). CC only flashes the firmware; Mayhem's own on-device apps transmit — **CC authors no TX payload**. New `hackrf_spiflash` backend; the flashable `.bin` is a ZIP member inside `FIRMWARE_mayhem_vX.Y.Z.zip` (variant pick: hackrf / portarf / hpro). Keep SHA `verify:`. |
| `meshcore` | ESP32 | esptool | Heltec LoRa32 V3, Station G2, Xiao ESP32-C3, M5 Unit C6L, LilyGo T-Beam (+ SX1262/SX1276 LoRa) | [meshcore-dev/MeshCore](https://github.com/meshcore-dev/MeshCore) | Mainline MeshCore companion LoRa mesh. Distinct from the MCLite build already documented; needs an external LoRa module per board. |
| `nautilus` | ESP32 | esptool | LilyGo T-Embed CC1101, esp32s3, 16MB (+ CC1101) | [n0xa/Nautilus-Firmware](https://github.com/n0xa/Nautilus-Firmware) | Sub-GHz RF firmware; requires the onboard CC1101. |
| `nrf802154_sniffer` | Passive Zigbee/Thread sniffer | nrf_dfu | nRF52840 Dongle (PCA10059) — nrf52840 | [nordicsemi/nRF-Sniffer-for-802.15.4](https://github.com/nordicsemi/nRF-Sniffer-for-802.15.4) | Passive IEEE 802.15.4 / Zigbee / Thread capture into Wireshark (extcap); receive-only (🛡 — no transmit). Only the **Dongle (PCA10059)** is CC-flashable via Nordic DFU; the DK boards need J-Link/SWD and are excluded. License NOASSERTION → fetch-from-origin only, do not re-host the `.hex`. `nrf_dfu` is extended here (raw `.hex` → `nrfutil pkg generate` → DFU serial). Keep app_offset / SHA / pinned-commit `verify:`. |
| `nrf_bluenullifier2` | ESP32 | esptool | NodeMCU-32S (ESP32-WROOM-32) + nRF24L01 | [wirebits/nrfBlueNullifier](https://github.com/wirebits/nrfBlueNullifier) | ⚠ **LAB-ONLY / illegal to operate.** Flash-and-study only — no transmit or wiring/build content; lawful-use frame required. |
| `porkchop` | ESP32 (M5) | esptool | M5Cardputer (esp32s3, 8MB) | [0ct0sec/M5PORKCHOP](https://github.com/0ct0sec/M5PORKCHOP) | M5Cardputer tool; single board/chip. The profile's real-hardware flash offset was hardware-gated — confirm with owner whether to document it yet. |
| `rnode` | ESP32 | esptool | `verify:` supported boards from the profile variants + upstream | [markqvist/RNode_Firmware](https://github.com/markqvist/RNode_Firmware) | Reticulum LoRa radio interface. Board list comes from `variants_for_chip` — confirm before writing. |
| `rnode_nrf` | Reticulum LoRa transport (nRF52) | nrf_dfu | RAK4631 (WisBlock, SX1262), LilyGo T-Echo, Heltec Mesh Node T114 — nrf52840 | [markqvist/RNode_Firmware](https://github.com/markqvist/RNode_Firmware) | nRF52 sibling of the ESP32 `rnode` profile above — Reticulum (RNS) LoRa transport; no attack payload but a user-tunable TX radio → **authorized/licensed use only**. Nordic Secure DFU over serial; the per-board `.zip` is passed whole (board into serial DFU mode via double-tap reset; `adafruit-nrfutil` on PATH). `nrf_dfu` is bench-gated (zero prior HW-validated consumers); flash ≠ signed/provisioned RNode. |
| `sniffle` | BLE 4.x/5.x link-layer sniffer | cc2538_bsl | SONOFF CC2652P USB Dongle Plus (cc2652p1, primary); CatSniffer V3 (cc1352p7) | [nccgroup/Sniffle](https://github.com/nccgroup/Sniffle) | ⚠ **lab-only** — passive/active BLE5 link-layer sniffer for TI CC13xx/CC26xx; active scan + connection-follow + link-layer relay/MITM are active manipulation (label, never block; authorized testing only). CC flashes only; the host runs Sniffle's own python out-of-band. TI LaunchPads are JTAG/UniFlash-only and OUT of `cc2538_bsl` scope. Bench-gated `cc2538_bsl` consumer; keep offsets / baud / SHA `verify:`. |
| `whad_butterfly` | Multi-protocol BLE/Zigbee/ESB/Unifying/Mosart/ANT research fw | nrf_dfu (+ uf2) | Nordic nRF52840 Dongle PCA10059 (nrf_dfu), Makerdiary nRF52840 MDK USB Dongle (uf2) — nrf52840 | [whad-team/butterfly](https://github.com/whad-team/butterfly) | ⚠ **lab-only** — sniff/scan/hijack + PDU-inject across BLE, Zigbee, Enhanced ShockBurst, Logitech Unifying, Mosart and ANT, driven from the host `whad-client`; inject/hijack are active TX (label, never block; authorized research only). CC only flashes; the WHAD host authors any injected traffic — do NOT bundle `whad-client`. Two paths: Nordic dongle via `nrf_dfu`, Makerdiary MDK via `uf2` drag-drop. `nrf_dfu` bench-gated. |
| `wifi_duck` | ESP8266 | esptool | DSTIKE WiFi Duck / Malduino W (esp8266 half), 4MB | [SpacehuhnTech/WiFiDuck](https://github.com/SpacehuhnTech/WiFiDuck) | BadUSB / keystroke-injection; **authorized-use frame** (⚠). ESP8266 half; flashed @0x0. |
| `zstack_coordinator` | Zigbee 3.x coordinator/router firmware | cc2538_bsl | Sonoff ZBDongle-E (cc2652p), CC2652R7, CC2652RB, SLZB / ZigStar CC2652P | [Koenkk/Z-Stack-firmware](https://github.com/Koenkk/Z-Stack-firmware) | Zigbee 3.x coordinator (and router) firmware for TI CC2652/CC1352 dongles. A coordinator is a legit-protocol TX radio performing normal Zigbee operation — no deauth/jam/injection (danger: none). Every release asset is a `.zip` wrapping one `.hex` → the resolver must unzip before flashing. BSL entry differs per dongle; LaunchPads are XDS110/UniFlash-only (out of scope). Compiled binary carries TI's SimpleLink SDK license — flag before any mirror. `cc2538_bsl` consumer; keep offset / baud / SHA `verify:`. |

## How to add a guide
1. Write `guides/<name>.md` following an existing guide's shape (what to buy → build → flash → **Cyber Controller
   profile** → troubleshooting), citing the exact `src/config/profiles/<id>.json` facts. Mark unverified steps `verify:`.
2. Add the entry to the guide index **data**, then regenerate: `python tools/build_readme.py`; build the PDF:
   `python tools/build_pdfs.py`.
3. These read best when they're written against the real hardware, so verify each step on the actual device
   before publishing. Open an issue or draft PR to claim a profile before you start, so two people don't end up
   writing the same guide.
